my_funct = lambda(n) {
  if n <= 1 or n > 0 or n == true {
    return (1 + 2 * (6 - 1)) * 5;
  } else {
    return n + myFunct(n - 1);
  };
}(2);

string_example = "hello world";

main = lambda(a) {
  b = true;
  myFunct(10);
};
